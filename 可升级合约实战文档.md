# 可升级合约实战文档

## 一、项目概述

本项目包含两套完整的可升级合约（UUPS Proxy 模式）：

| 合约 | V1 功能 | V2 新增 |
|------|---------|---------|
| **MyUpgradeableNFT** | ERC-721 基础铸造 | tokenURI 存储、最大供应量、销毁、批量铸造 |
| **NFTMarketV1/V2** | 上架/购买/取消 + 手续费 | EIP-712 离线签名上架（一次 setApprovalForAll 即可） |

## 二、项目文件结构

```
src/
├── MyUpgradeableNFT.sol              # ERC-721 V1（可升级）
├── MyUpgradeableNFT_V2.sol           # ERC-721 V2（继承 V1）
├── NFTMarketV1.sol                   # NFT 市场 V1（可升级）
├── NFTMarketV2.sol                   # NFT 市场 V2（继承 V1 + 签名上架）
├── mock/
│   ├── MockERC20Permit.sol           # 测试 ERC-20 + EIP-2612 Permit
│   └── MockNFT.sol                   # 测试 ERC-721
├── AirdropMerkleNFTMarket.sol        # Merkle + Permit + Multicall 市场
├── Bank.sol                          # 存款排行榜
└── Whitelist.sol                     # Merkle 白名单

script/
├── DeployMyNFT.s.sol                 # 部署 MyUpgradeableNFT
├── UpgradeMyNFT.s.sol                # 升级 MyUpgradeableNFT
├── generateMerkleTree.ts             # Merkle 树生成
└── signPermit.ts                     # EIP-712 Permit 离线签名

test/
├── MyUpgradeableNFT.t.sol            # 13 项测试
├── NFTMarketUpgradeable.t.sol        # 17 项测试
└── AirdropMerkleNFTMarket.t.sol      # 10 项测试
```

## 三、UUPS 代理架构原理

### 3.1 核心结构

```
用户 ──调用──→ Proxy (ERC1967Proxy) ──delegatecall──→ Implementation (逻辑合约)
                 │                                      │
                 │ 持有所有状态 (storage)                 │ 提供所有逻辑 (code)
                 │ 地址永久不变                           │ 可随时替换
                 │                                      │
                 └── EIP-1967 标准存储槽 ────────────────┘
                     • 0x360894a13ba1a321... ← implementation 地址
                     • 0xb53127684a568b31... ← admin 地址
```

### 3.2 升级流程

```
1. 部署 Implementation V1
2. 部署 ERC1967Proxy(implV1, initData)  ← 代理 + 原子初始化
3. 用户与 Proxy 交互
4. 部署 Implementation V2（新逻辑合约）
5. Proxy.upgradeToAndCall(implV2, "")
   → 修改 storage slot 0x3608... → implV2 地址
   → 所有状态保留，逻辑切换
```

### 3.3 delegateCall 存储布局规则

```
V1 存储布局:                    V2 存储布局（正确）:
┌────┬──────────────┐          ┌────┬──────────────┐
│  0 │ _nextTokenId │          │  0 │ _nextTokenId │ ← 不变
│  1 │ Ownable slot │          │  1 │ Ownable slot │ ← 不变
│  2 │ ERC1967 slot │          │  2 │ ERC1967 slot │ ← 不变
│ ... │ ...         │          │ ... │ ...         │ ← 不变
│ 51 │ __gap[0..49] │          │ 51 │ __gap[0..49] │ ← 不变
└────┴──────────────┘          │101 │ _tokenURIs   │ ← V2 新增
                               │102 │ maxSupply    │ ← V2 新增
                               └────┴──────────────┘

存储布局铁律：
✅ 新变量只能追加在末尾
✅ 类型不能改变 (uint8 → uint256 ❌)
✅ 不能删除变量（用 __gap 预留占位）
✅ 不能改变变量顺序
✅ 继承顺序不能变
```

## 四、MyUpgradeableNFT 合约详解

### 4.1 V1 合约设计

```solidity
contract MyUpgradeableNFT is UUPSUpgradeable, OwnableUpgradeable, ERC721Upgradeable {
    uint256 private _nextTokenId;           // token ID 计数器
    uint256[50] private __gap;              // 预留 50 个 slot

    constructor() { _disableInitializers(); } // 锁定实现合约

    function initialize(address _owner, string memory _name, string memory _symbol)
        external initializer { ... }

    function safeMint(address to) public onlyOwner returns (uint256) { ... }
    function totalMinted() public view returns (uint256) { ... }
    function _authorizeUpgrade(address) internal override onlyOwner {}
}
```

关键设计：
- `_disableInitializers()` — 防止攻击者直接调用 Implementation 的 `initialize()`
- `initializer` 修饰符 — 保证 `initialize` 只执行一次
- `__gap` — 预留存储空间给未来版本
- `_authorizeUpgrade` — 只有 owner 可以升级

### 4.2 V2 合约设计

```solidity
contract MyUpgradeableNFT_V2 is MyUpgradeableNFT {
    // 新变量追加在末尾
    mapping(uint256 => string) private _tokenURIs;
    uint256 public maxSupply;

    // 新增函数
    function safeMint(address to, string calldata uri) public onlyOwner returns (uint256);
    function burn(uint256 tokenId) external;
    function setMaxSupply(uint256 _max) external onlyOwner;
    function batchMint(address to, uint256 quantity, string calldata uri) external onlyOwner;
    function tokenURI(uint256 tokenId) public view override returns (string memory);
}
```

V1 的 `totalMinted()` 和 `safeMint()` 必须是 `public`（不是 `external`），V2 才能内部调用。

### 4.3 部署与升级命令

```bash
# 部署 V1
forge script script/DeployMyNFT.s.sol \
  --rpc-url http://localhost:8545 --broadcast \
  --private-key 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# 升级到 V2
PROXY_ADDRESS=0x... forge script script/UpgradeMyNFT.s.sol \
  --rpc-url http://localhost:8545 --broadcast \
  --private-key 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# 验证存储布局
forge inspect src/MyUpgradeableNFT.sol:MyUpgradeableNFT storage-layout --pretty
forge inspect src/MyUpgradeableNFT_V2.sol:MyUpgradeableNFT_V2 storage-layout --pretty
```

## 五、NFTMarketV1/V2 合约详解

### 5.1 V1 合约设计

```solidity
contract NFTMarketV1 is UUPSUpgradeable, OwnableUpgradeable, ReentrancyGuard {
    struct Listing { address seller; uint256 price; bool active; }

    mapping(address => mapping(uint256 => Listing)) internal _listings;
    uint256 public platformFeeBps;
    uint256 public platformBalance;
    uint256[50] private __gap;

    function list(address nft, uint256 tokenId, uint256 price) external;
    function buy(address nft, uint256 tokenId) external payable nonReentrant;
    function cancel(address nft, uint256 tokenId) external;
}
```

V1 需要用户为每个 NFT 单独调用 `approve(market, tokenId)`。

**手续费机制**：
- `platformFeeBps` 基点（250 = 2.5%）
- `buy()` 时自动从售价中扣除
- owner 通过 `withdrawPlatformBalance()` 提取

### 5.2 V2 合约设计：签名上架原理

```
传统方式（V1）：
  approve(NFT₁) → list(NFT₁)      ← 每个 NFT 两笔交易
  approve(NFT₂) → list(NFT₂)
  approve(NFT₃) → list(NFT₃)

V2 签名方式：
  setApprovalForAll(market, true)  ← 一笔交易，永久授权
  签名(NFT₁) → listWithSignature() ← 免费（中继器付 gas）
  签名(NFT₂) → listWithSignature()
  签名(NFT₃) → listWithSignature()
```

**EIP-712 签名结构**：

```
签名内容：ListNFT(address nftContract, uint256 tokenId, uint256 price, uint256 nonce, uint256 deadline)

Domain: {
  name: "NFTMarket",
  version: "2",
  chainId: 31337,
  verifyingContract: marketAddress
}
```

**签名验证流程**：

```
listWithSignature(seller, nftContract, tokenId, price, deadline, v, r, s)
  │
  ├─ 1. price > 0
  ├─ 2. deadline 未过期
  ├─ 3. seller 已 setApprovalForAll(market)
  ├─ 4. seller 是 NFT owner
  ├─ 5. 该 NFT 未重复上架
  │
  ├─ 6. structHash = keccak(ListNFT(contract, id, price, nonce, deadline))
  │      digest = keccak(\\x19\\x01 ‖ domainSeparator ‖ structHash)
  │      signer = ecrecover(digest, v, r, s)
  │      require(signer == seller) ← 签名验证
  │
  ├─ 7. sigNonces[seller]++  ← 防重放
  └─ 8. 创建 Listing
```

**安全机制**：

| 防护 | 机制 |
|------|------|
| 重放攻击 | `sigNonces[seller]` 每次递增，同一签名只能用一次 |
| 过期签名 | `deadline` 超时后签名失效 |
| 跨合约滥用 | EIP-712 Domain 绑定合约地址 + chainId |
| 冒名上架 | `ecrecover` 恢复签名者，必须等于 seller |
| 双重上架 | `_listings[id].active` 去重检查 |

### 5.3 测试覆盖

```
NFTMarketUpgradeable 测试 (17 项):
├── V1 基础 (7 项)
│   ├── test_V1_List           — 正常上架
│   ├── test_V1_Buy            — 购买 + 手续费分成
│   ├── test_V1_Cancel         — 取消上架
│   ├── test_V1_Revert_NotOwner — 非持有者上架失败
│   ├── test_V1_Version        — 版本号为 1
│   ├── test_V1_Admin_SetFee   — 修改手续费
│   └── test_V1_Admin_Withdraw — 提取平台收益
│
├── 升级验证 (2 项)
│   ├── test_Upgrade_Version        — 版本号变为 2
│   └── test_Upgrade_StatePreserved — V1 上架数据 + 手续费在 V2 保留
│
├── V2 签名上架 (7 项)
│   ├── test_V2_ListWithSignature             — 中继器代提交签名
│   ├── test_V2_SignatureListing_Buy          — 签名上架后正常购买
│   ├── test_V2_SignatureListing_MultipleNFTs — 一次授权多签名上架
│   ├── test_V2_SignatureListing_NonceReplay  — nonce 防重放
│   ├── test_V2_SignatureListing_ExpiredDeadline — deadline 防过期
│   ├── test_V2_SignatureListing_WrongSigner  — 错签名验证失败
│   └── test_V2_SignatureListing_NotApproved  — 未授权失败
│
└── 升级安全 (1 项)
    └── test_V2_UpgradeOnlyOwner — 非 owner 升级失败
```

## 六、升级安全清单

| # | 检查项 | 状态 |
|---|--------|:--:|
| 1 | `_disableInitializers()` 在构造函数中调用 | ✅ |
| 2 | `initializer` 修饰 `initialize()` | ✅ |
| 3 | V2 继承 V1，不破坏存储布局 | ✅ |
| 4 | `__gap` 预留未来存储空间 | ✅ uint256[50] |
| 5 | `_authorizeUpgrade` 有权限控制 | ✅ onlyOwner |
| 6 | 升级后 V1 状态完整保留 | ✅ 测试通过 |
| 7 | 升级后 V1 功能正常 | ✅ 测试通过 |
| 8 | V2 新功能正常工作 | ✅ 测试通过 |
| 9 | 存储布局对比无冲突 | ✅ |
| 10 | 非 owner 无法升级 | ✅ 测试通过 |

## 七、常见问题与解决

### Q1: `external` vs `public` 在派生合约中的影响？

```solidity
// V1
function totalMinted() external view returns (uint256); // ❌ V2 内部无法调用

// 修复
function totalMinted() public view returns (uint256);    // ✅ V2 可内部调用
```

### Q2: `vm.prank` 被 `new` 消耗？

```solidity
// ❌ vm.prank 会被 new 消耗
vm.prank(owner);
NFTMarketV2 newImpl = new NFTMarketV2();   // ← prank 被消耗
proxy.upgradeToAndCall(...);                // ← 实际调用者是 test 合约

// ✅ 正确：new 在前，prank 在后
NFTMarketV2 newImpl = new NFTMarketV2();
vm.prank(owner);
proxy.upgradeToAndCall(address(newImpl), "");
```

### Q3: OZ v5.6 移除了哪些 upgradeable 合约？

- `ReentrancyGuardUpgradeable` → 使用 `ReentrancyGuard`（namespace storage，无需初始化）
- `__UUPSUpgradeable_init()` → 不再需要显式调用

### Q4: 如何防止实现合约被直接初始化？

```solidity
constructor() {
    _disableInitializers();  // 锁定实现合约的 initialize
}
```

## 八、运行测试

```bash
# 所有可升级合约测试
forge test --match-path test/MyUpgradeableNFT.t.sol -v
forge test --match-path test/NFTMarketUpgradeable.t.sol -v

# 查看存储布局
forge inspect src/MyUpgradeableNFT.sol:MyUpgradeableNFT storage-layout --pretty
forge inspect src/MyUpgradeableNFT_V2.sol:MyUpgradeableNFT_V2 storage-layout --pretty
```
